# Desktop / CI (dummy VLM + API)
numpy>=1.24
opencv-python-headless>=4.8
Pillow>=10.0
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# Optional on the Jetson Orin Nano 8 GB (INT4 Qwen2.5-VL-3B)
# torch  (JetPack wheel)
# transformers>=4.49
# accelerate
# bitsandbytes          # 4-bit on x86; on Jetson prefer TensorRT-LLM / Ollama
# ultralytics           # YOLOv8n → TensorRT .engine
# httpx
